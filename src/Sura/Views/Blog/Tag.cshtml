@model IEnumerable<Sura.Views.Blog.Models.Post>
@{
    ViewBag.Title = "Tag: " + ViewBag.Tag;
}
<h2>Tag: @ViewBag.Tag (@Model.Count() @Html.Raw("post")@if(Model.Count() > 1){@Html.Raw("s")})</h2>

@foreach (var post in Model)
{
  @Html.Partial("_Post", post)
}
